# 南投鹿谷 天氣資訊網站

這是一個展示南投鹿谷天氣資訊、預報和知名景點的網站。

## 功能特色

- 🌤️ 即時天氣資訊顯示
- 📅 未來 7 天天氣預報
- 📍 鹿谷知名景點介紹
- 📤 分享功能
- 📸 圖片匯出功能
- 💌 景點推薦功能

## 設定步驟

### 1. 取得天氣 API 授權碼

1. 前往 [中央氣象署開放資料平台](https://opendata.cwa.gov.tw/user/login)
2. 註冊帳號並登入
3. 取得 API 授權碼

### 2. 設定 API 密鑰

1. 複製 `config.example.js` 檔案
2. 重新命名為 `config.js`
3. 打開 `config.js`
4. 將 `YOUR_API_KEY` 替換為您取得的實際 API 授權碼

```javascript
const WEATHER_API_URL = 'https://opendata.cwa.gov.tw/api/v1/rest/datastore/F-D0047-023?Authorization=YOUR_API_KEY';
```

### 3. 重要提醒

⚠️ **請勿將 `config.js` 提交到 GitHub！**

- `config.js` 已加入到 `.gitignore` 中
- 只提交 `config.example.js` 作為範本
- 確保您的 API 密鑰不會洩露

## 檔案結構

```
├── index.html          # 主頁面
├── script.js           # JavaScript 功能
├── styles.css          # 樣式表
├── attractions.json    # 景點資料
├── config.js           # API 設定（不提交到 GitHub）
├── config.example.js   # API 設定範例
├── .gitignore         # Git 忽略檔案
├── README.md          # 說明文件
└── image/             # 圖片資料夾
    ├── 1.png
    ├── 2.png
    └── ...
```

## 使用方式

1. 開啟 `index.html` 即可使用
2. 切換不同分頁查看天氣資訊、預報和景點
3. 可以匯出圖片或分享內容
4. 可以推薦新的景點

## 聯絡方式

如有問題或建議，請聯絡：makerbackup0821@gmail.com

## GitHub Pages 部署

### 方法一：使用 GitHub Actions（推薦）

1. **設定 GitHub Secrets**
   - 前往您的 GitHub 倉庫
   - 點擊 `Settings` > `Secrets and variables` > `Actions`
   - 點擊 `New repository secret`
   - Name: `WEATHER_API_KEY`
   - Value: 貼上您的 API 授權碼
   - 點擊 `Add secret`

2. **啟用 GitHub Pages**
   - 前往 `Settings` > `Pages`
   - Source: 選擇 `GitHub Actions`
   - 儲存設定

3. **推送程式碼**
   - 將程式碼推送到 `main` 或 `master` 分支
   - GitHub Actions 會自動部署
   - 部署完成後，網站會自動更新

### 方法二：手動部署

1. **建立 config.js**
   - 在本地建立 `config.js` 檔案
   - 填入您的 API 密鑰
   - **注意：此方法會將 API 密鑰公開在 GitHub 上**

2. **啟用 GitHub Pages**
   - 前往 `Settings` > `Pages`
   - Source: 選擇 `Deploy from a branch`
   - Branch: 選擇 `main` 或 `master`，資料夾選擇 `/ (root)`
   - 儲存設定

3. **推送程式碼**
   - 將所有檔案（包括 config.js）推送到 GitHub
   - 網站會自動部署

### 部署後的網址

部署完成後，您的網站網址會是：
```
https://[您的GitHub使用者名稱].github.io/[倉庫名稱]/
```

例如：
```
https://username.github.io/lugu-weather/
```

## 授權

此專案僅供學習和個人使用。

